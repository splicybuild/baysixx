document.querySelectorAll('a[href^="#"]').forEach((link)=>{link.addEventListener('click',(e)=>{const id=link.getAttribute('href');if(id==='#')return;const target=document.querySelector(id);if(target){e.preventDefault();target.scrollIntoView({behavior:'smooth',block:'start'});}})});
const backTop=document.querySelector('.back-to-top');if(backTop){const toggle=()=>backTop.classList.toggle('visible',window.scrollY>500);window.addEventListener('scroll',toggle,{passive:true});toggle();}
const mobileNav=document.createElement('nav');mobileNav.className='mobile-bottom-nav';mobileNav.setAttribute('aria-label','Mobile navigation');const path=location.pathname;const onHome=path.endsWith('index.html')||path==='/'||path.endsWith('/baysixx/');const onShop=path.endsWith('shop.html')||path.endsWith('product.html');const onStory=path.endsWith('story.html');mobileNav.innerHTML=`<a class="${onHome?'active':''}" href="index.html">HOME</a><a class="${onShop?'active':''}" href="shop.html">SHOP</a><a href="${onHome?'#drop':'index.html#drop'}">COLLECTIONS</a><a class="${onStory?'active':''}" href="story.html">ABOUT</a>`;document.body.appendChild(mobileNav);